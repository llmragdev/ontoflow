# 용어집

[English](GLOSSARY.en.md)

이 용어집은 사용자가 먼저 이해할 수 있는 표현을 앞에 두고, 설계 문서에서 쓰는 기술 용어를 괄호 안에 함께 적습니다.

## 용어 사용 원칙

OntoFlow는 `TBox / ABox`를 온톨로지와 Description Logic 계열에서 쓰이는 일반적인 타입/인스턴스 구분으로 사용합니다.

- `TBox`는 클래스 타입과 정의 영역입니다.
- `ABox`는 실제 인스턴스와 데이터 영역입니다.
- `ObjectType`은 업무 객체의 클래스 타입입니다.
- `ObjectInstance`는 실제 업무 객체 인스턴스입니다.
- `RelationType`은 객체 타입 간 관계 정의입니다.
- `ActionType`은 실행 가능한 업무 작업 정의입니다.

이 용어들은 특정 제품만의 고유 명칭이라기보다, 온톨로지의 타입/인스턴스 구분과 엔터프라이즈 업무 모델링에서 자연스럽게 쓰이는 명명 체계입니다.

## OntoFlow

한국어로는 **온토플로우**라고 읽습니다. `Ontology`와 `Flow`를 합친 이름입니다.

업무 데이터 모델과 업무 절차 실행을 연결해, 값이 왜 바뀌었는지 추적할 수 있게 만드는 업무 자동화 아키텍처입니다.

## Ontology Manager

업무 데이터 모델 관리기입니다. 업무 객체, 속성, 관계, 대량 등록, 변경 이력을 관리합니다.

## Workflow Manager

업무 절차 실행기입니다. 업무 단계, 입력값, 상태 변화, 외부 호출, 실행 로그를 관리합니다.

## ObjectType

업무에서 다루는 대상의 클래스 타입입니다. 예: `Patient`, `Doctor`, `Appointment`, `Equipment`, `WorkOrder`.

## ObjectInstance

`ObjectType`에서 정의한 클래스 타입으로 만들어진 실제 개별 데이터입니다. 예: 특정 환자, 특정 예약, 특정 설비.

## PropertyDefinition

업무 대상이 가지는 항목의 정의입니다. 예: 환자 이름, 예약 시간, 설비 상태. 값 형식, 필수 여부, 허용값 같은 검증 기준을 가집니다.

## RelationType

두 업무 대상 사이의 관계 정의입니다. 예: 환자가 예약을 가진다, 의사가 진료를 담당한다.

## ActionType

실행할 수 있는 작업의 약속입니다. 어떤 입력이 필요하고, 어떤 결과가 나오며, 어떤 외부 시스템을 호출하는지 정의합니다.

## State Slot

업무 절차가 진행되는 동안 임시로 보관하는 값입니다. 예: 선택된 환자, 희망 예약일, 조회된 가능 시간.

## State Slot History

업무 진행 중 임시 값이 언제, 어떤 단계에서, 무엇으로 바뀌었는지 남기는 기록입니다.

## Persistent Knowledge

실제로 저장되는 업무 데이터입니다. 업무 대상, 속성값, 관계, 변경 이력을 포함합니다.

## Apply Boundary

실제 업무 데이터를 바꾸는 공통 반영 통로입니다. 대량 등록, 직접 수정, 업무 절차 결과 반영은 모두 이 통로를 지납니다.

## Unified Validator

공통 검증기입니다. 입력값, 대량 등록, 직접 수정, 사전 점검이 모두 같은 기준으로 검사되게 합니다.

## Import Job

파일이나 API로 들어온 많은 데이터를 검증하고 반영하기 위한 작업 단위입니다.

## TBox

클래스 타입 영역입니다. 어떤 업무 대상의 종류가 있고, 각 종류가 어떤 속성, 관계, 작업 규칙을 가지는지 정의합니다.

## ABox

인스턴스 영역입니다. `TBox`에서 정의한 클래스 타입에 따라 만들어진 실제 환자, 예약, 설비, 작업 지시 같은 개별 데이터를 담습니다.

## Gate

업무 절차가 만든 변경 후보를 사람이 승인할 수 있게 올리는 확장 기능입니다.

## Change Candidate

아직 실제 데이터에 반영되지 않은 변경 제안입니다. 승인될 수도 있고 반려될 수도 있습니다.

## Ontology Change History

업무 데이터 정의나 실제 업무 데이터가 바뀐 사실을 남기는 변경 기록입니다.

## Conversational Input

사용자의 말을 업무 절차 후보와 입력값 후보로 바꾸는 앞단 입력 기능입니다. 이 기능이 직접 데이터를 바꾸지는 않습니다.

## Dry-Run

실제 반영 없이 미리 실행해 보는 모드입니다. 누락값, 검증 실패, 예상 변경을 확인할 때 씁니다.
